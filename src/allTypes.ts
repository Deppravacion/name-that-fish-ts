export type TClassProps = {
  state: {
    correctCount: number;
    incorrectCount: number;
  }
}

export type TComponentProps = {
  fishState: {
    correctScore: number;
    incorrectScore: number;
  }
}